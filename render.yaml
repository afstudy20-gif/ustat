# Deprecated — uSTAT has migrated away from Render.
# This file is kept for historical reference only.
#
# Recommended deployment platform: Coolify (https://coolify.io)
# See docker-compose.yml and README.md → Self-hosting section.

services:
  - type: web
    name: wiz3
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
