[pytest]
# tests/ is a package (has __init__.py), so pytest does not auto-insert it on
# sys.path. The test modules import the shared fixtures via `from conftest
# import ...`, so add tests/ to the path explicitly. Without this, collection
# fails with "ModuleNotFoundError: No module named 'conftest'".
pythonpath = tests
testpaths = tests
